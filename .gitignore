# Dependencies
node_modules
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Scaffold leftovers from create-next-app / next dev (do not publish)
AGENTS.md
CLAUDE.md

# Next.js
.next
out
build
dist

# Env (never commit secrets)
.env
.env*.local
!.env.example

# Local credential dumps (never publish)
assumer.env
*-assumer.env
/tmp/*.env

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# OS / IDE
.DS_Store
*.pem
!certs/*.pem
.idea
.vscode/*
!.vscode/extensions.json

# Test / coverage
coverage
*.tsbuildinfo
next-env.d.ts

# Terraform (Step G) — keep examples, ignore state
**/.terraform/
*.tfstate
*.tfstate.*
tfplan
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
terraform/backend.hcl
terraform/terraform.tfvars

